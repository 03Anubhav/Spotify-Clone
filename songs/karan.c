#include <stdio.h>
#include <math.h>

int main(int argc, char const *argv[])
{
    // int age = 0;
    // char fruit = 'f';

    // scanf("%d", &age); // Read an integer and store it in 'age'

    // // Print the age
    // printf("You entered: %d \n", age);
    // printf("You entered: %d as your age", age);


    // Area of Circle
    int radius = 7;
    float AreaCircle = 3.14 * pow(radius, 2);

    printf("The area of the circle is : %f", AreaCircle);
    return 0;
}
// int, float, boolean, string, char